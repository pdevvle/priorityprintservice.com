node_modules/
package-lock.json
package.json

# Plugin zips built by tools-plugin-zip.sh — artifacts, rebuilt on demand
dist/

# Measured price floors — real pricing figures. The repo is public (CLAUDE.md:
# "treat every branch as public: don't commit pricing figures, strategy, or
# credentials"). Sweep output stays local and goes into wp_options, not git.
docs/min-prices.json
min-prices.json
.minprice-*.html
# Scratch calculator builds written by the headless test harnesses — a copy of a
# calculator with its CDN scripts swapped for ./node_modules paths. Regenerated
# on every run, and committing one would put a stale duplicate of a calculator
# in the tree.
.t4o*.html
